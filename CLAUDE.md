# CLAUDE.md — Mind-Palace Operating Rules

This file is loaded every session. It is the operational layer. The full design lives in `docs/BUILD-SPEC.md`; the inviolable directives every agent inherits live in `CONSTITUTION.md`; engineering and security practice lives in `CONVENTIONS.md`. Read all three before writing code.

## What this project is
A single-user, offline-first, privacy-sealed personal AI — a "mind palace" that indexes the owner's private notes and reflects patterns back, plus a gated agent layer, a dynamic agent factory, a pluggable messaging interface, and a one-way research airlock. Built to be **extended over time**, not shipped as a fixed product. Owner is a security-focused DevOps/AWS engineer with strong Python — write to that level.

## Non-negotiables (full list in BUILD-SPEC §3; never violate)
1. **Sealed core has zero network egress.** Enforce structurally, not by convention.
2. **Network and private data never share a component.** Only `edge/` (bridge + interface gateway) touches the network; neither reads the vault.
3. **The model advises; code acts.** No model holds a shell, raw secrets, or direct infra mutation.
4. **Executed code is powerless.** Runs sandboxed: no creds, no network (unless explicit scoped grant), no vault. Returns data, never actions.
5. **Self-modification is gated → validated → reversible.** Propose → human-approve → execute → validate → auto-rollback. No step skipped.
6. **Every agent inherits `CONSTITUTION.md`** as its outermost frame. Task instructions nest inside, never override. Minted agents can't exceed their template's scope or a pre-declared max.
7. **Consequential advice (health/financial/legal) defers but isn't withheld** — substantive + honest about uncertainty + refuses dangerous specifics + final decision to owner & a professional.
8. **Respect the memory ceiling** (§5): ≤2 resident models, ~20–24 GB usable. Scheduler refuses breaching work.
9. **The fixed points are sacred** — the frozen golden set and `CONSTITUTION.md` are never auto-modified. Human-only, deliberate, logged.
10. **Secrets outside code** — Keychain/env only; never commit, read-by-model, or log.
11. **The interface may transit a third party; the corpus never does.** WhatsApp-style adapters leak interactions, not the corpus — opt-in only; private default is local/Tailscale.
12. **Voice/telephony is bounded.** Speech synth/recognition run locally in core; only audio crosses the carrier. The adapter dials **only the owner's pre-registered number**; the LLM never supplies a number; calls are owner-initiated; a passphrase/callback authenticates the human before personalized content is spoken.

## How to work
- **Build phase by phase** (BUILD-SPEC §18). Verify each phase against its gate, then **stop and checkpoint with the human** before advancing. Do not jump ahead.
- **Ask, don't guess** on the decisions in BUILD-SPEC §20. For everything else, pick a sensible default and state it inline.
- **Write tests as you build**, not after. Small, verifiable steps.
- If a feature can't be built without violating §3 or the Constitution, **stop and surface it**.

## Build-session budget (you, the building agent)
This is a large system — build it without exhausting your own context. (Distinct from the runtime context budgeter in BUILD-SPEC §13, which manages the *local* models at run time.)
- **One phase per session.** Build a single phase, verify, hand off; the human starts a fresh session (or `/clear`) for the next. Don't carry the whole history.
- **Resume from `docs/PROGRESS.md`, not from chat history.** At each checkpoint append a terse entry there (built / verified / next / decisions). A fresh session re-grounds from that file + this one + the current phase's spec section.
- **Update the Current phase marker** below as phases complete.
- **Reference, don't echo.** Don't paste large file contents back; cite paths and work incrementally.

## Conventions (full detail in CONVENTIONS.md)
- **Python** for orchestration/agents. Thin custom code over heavy frameworks — **no LangGraph/CrewAI/AutoGen**; own the loop.
- **Stores:** LanceDB (vectors), DuckDB (telemetry), SQLite (queue/state/gate). Scoped access per agent — enforce in an access layer.
- **Model serving:** Ollama (HTTP). Keep it abstract so a future GPU node can join.
- **AWS = Terraform only.** No click-ops. Least-privilege IAM.
- **Sandbox:** rootless Podman (network-off, no-mount, limited) + warm pool; WASM for pure compute.
- **Comment the *why* at trust boundaries** (airlock asymmetry, propose/execute split, scope ceiling).

## Repo map
```
core/    Zone A — sealed, no network (librarian, curator, dreaming, ingest, matching, factory, sandbox, stores)
edge/    Zone B — networked, containerized (bridge, interface)
cloud/   Zone C — Terraform + fetcher
agents/  persistent role definitions      scheduler/  supervisor + queue + context budgeter
ops/     gate, levers, rollback           eval/  golden sets, metrics, baselines
docs/    BUILD-SPEC.md, PROGRESS.md (build log)
```
Keep `core/` free of any import that can reach the network.

## Current phase
> **NOW (2026-06-28): ALL TEN NUMBERED PHASES (0–10) COMPLETE.** The main build is DONE. Phase 10 — the self-modification loop — landed this session: propose→approve→execute→validate→rollback (`ops/levers.py` + `ops/ledger.py` + `ops/apply.py` + `ops/selfmod.py` + `ops/selfmod_cli.py`, `[selfmod]` config). **Owner's scoping steer (held structurally):** the loop may tune **bounded numeric config knobs ONLY** (the `[dreaming]` levers); a `ProposedChange` has no field that can carry a path/diff/command, so **code/infra changes are unrepresentable, not just disallowed**. Validate uses the **frozen golden anchor** (capability) + rolling baseline (drift) via `gate_admits`; `conforms` (behavioral) stays honestly deferred. Mechanical rollback restores the prior knob from a machine-owned `config/levers.toml` overlay (human `local.toml` always wins). **Fail-closed OFF by default**: `[selfmod] enabled` master switch + `unattended_enabled` (the no-human "safe levers" path) both off; NOT wired into cron (nothing autonomously proposes yet). Gate verified: good change kept, bad change auto-rolls-back, boiling-frog frozen-anchor catch — **logic suite 350 (+37)**. Remaining work is operational/optional only (activate `[selfmod]`, wire a cautious proposer, Phase 6b voice §20.11, empirical `-m podman`). Done earlier: security & attestation track + **production Vault** (kv + AWS dynamic engine, auto-unseal) + **AWS airlock applied** + **Phase 9 Backups** deployed live (restic→S3+SSE-KMS, snapshot `fbb9935a`, daily 03:30 LaunchAgent). `[secrets]`/`[backup]`/`[selfmod]` are per-machine via the gitignored `config/local.toml` overlay. Detail below is HISTORY.
> **Near-term task (2026-06-27, NOT a phase — main build still at Phase 9 next, NOT started): Vault watcher & sync — OPERATIONALLY COMPLETE.** Code built 2026-06-26: `core/ingest/sync.py` `VaultSync` (incremental re-ingest through the Phase-1 pipeline; idempotent via content-addressing — unchanged=no-op, changed=re-embed, **delete=tombstone**: derived dropped, catalog inactive, **raw KEPT**; dedup-safe via `VaultCatalog.active_refs`), `core/stores/catalog.py` `VaultCatalog` (active/tombstone ledger), `core/ingest/watch.py` `VaultWatcher` (**core-side, local-fs only, no network**; `watchdog` optional+lazy, poll fallback; injected `on_change` so core never imports scheduler), `core/ingest/purge.py` `purge_raw` (owner-gated true deletion). Wired via `scheduler/vault_sync.py` (background `vault_sync` job → **pinned** tier). This session (2026-06-27) did the **operational setup + live e2e verification**, on the owner's Mac (M2 Max) + iPhone: Tailscale tailnet joined (both devices, direct path confirmed via `tailscale ping`, no DERP relay); Syncthing on Mac (`brew services`) + Synctrain on iPhone, vault folder shared, **confined to the tailnet** (global discovery/relays/NAT/STUN disabled both ends, peer addresses pinned to Tailscale IPs, confirmed direct-not-relayed even over cellular); watcher installed as a macOS LaunchAgent (`~/Library/LaunchAgents/com.mind-palace.watch.plist`, `RunAtLoad`+`KeepAlive`, `watchdog` real-time backend) — manage via `launchctl bootstrap|bootout gui/$(id -u)[/com.mind-palace.watch]`, logs at `data/logs/watch.{out,err}.log`. **Bug found + fixed:** `scheduler/queue.py` `JobQueue` crashed silently (service looked healthy, never actually enqueued anything) because its SQLite connection was main-thread-only while the watcher's debounce/poll fire `on_change` from a spawned thread; fixed with `check_same_thread=False` + a reentrant `RLock` guarding every method. iOS capture via a Shortcut (`docs/runbook.md` has the exact action wiring, including a workaround for Shortcuts forcing `.txt` on `Save File` regardless of typed extension). **247 logic pass (+5)**; import-lint green; ruff clean; **live-verified** (real devices, not mocks): phone note → synced → auto-ingested → top search hit; delete → auto-tombstoned (raw confirmed retained); unchanged rescan → no-op. Full session log in `docs/PROGRESS.md`. `[vault] watch_*` + `paths.vault_catalog` config; `watchdog` now installed.
> Phase: 8 COMPLETE (2026-06-26) — the airlock (AWS Zone C), verified against gate (**225 logic passed (+31)**; `test_research_live` PASSED with the embedder; 13 deselected). One-way research flow (§16): core emits **de-identified criteria** → filesystem handoff → Zone-B **bridge** PUTs to S3 `requests/` → cloud **fetcher** (Lambda) writes public literature to S3 `results/` → bridge GETs → core **ranks inside the walls**. `core/research/`: `criteria.py` (the privacy boundary — `ResearchCriteria` has **no field that can carry note content**; `deidentify()` conservative scrubber raises-rather-than-passes-doubt; `assert_clean()` re-checks at emit), `airlock.py` (core-side handoff, **no S3/network/`import edge`**), `rank.py` (personalize public papers vs the AUTHORED-note centroid — mirror firewall holds — **transient, never ingested into the mirror**; evidence tiers + preprint/unresolved-id flags). Librarian `research_criteria()` seam realized (deterministic proposer default; `model_term_proposer` injectable). `edge/bridge/`: `ResearchBridge` = the ONLY S3-touching component, a **dumb pipe with no vault handle**, `boto3` lazy in `build_bridge` (core never sees it; `edge` optional dep). `cloud/fetcher/`: key-free OpenAlex/EuropePMC/arXiv, stdlib-only dependency-free zip, dedup+evidence-bias+drop-unresolvable. `cloud/terraform/bootstrap` (fresh dedicated `mind-palace-tfstate-*`, local state, S3-native locking, no DynamoDB) + `cloud/terraform/airlock` (S3 `requests/`+`results/`, fetcher Lambda **no-VPC=public-egress-only**, S3-PUT trigger, **least-privilege IAM**: fetcher=GetObject requests/* + PutObject results/* + logs; bridge=PutObject requests/* + GetObject/List results/*). `[airlock]` config + `AirlockConfig`. **`terraform validate`+`tflint`+`fmt` clean on both**; import-firewall OK (core has zero S3/boto3/edge reach). ⚠️ **`terraform apply` NOT run** (operational boundary — owner applies after `aws sso login --profile alberto-sso`; copy `airlock_bucket`→config, set up `mind-palace-bridge` profile assuming `bridge_role_arn`; recommended: narrow `bridge_trusted_principal_arns` to the SSO role; steps in `cloud/terraform/airlock/README.md`). ⚠️ Phase 4 empirical `-m podman` still pending — see `docs/runbook.md`. Next: **Phase 9 — Backups** (restic → S3 + SSE-KMS, scheduled; AWS sees no plaintext; §16b). Phase 6b voice optional (§20.11). Update this line as phases complete.
> _Prior (see PROGRESS.md for full detail):_ Phase 7 — Curator + dreaming on the **INTERPRETED** layer (`DerivedStore` writes `interpreted` only — structural §8 firewall; `Dreamer`/`Curator` mirror-not-oracle, flag-never-rewrite-authored), trough-only via the Phase-3 foreground gate. Hardening pass (NOT a phase) — invariant catalog promoted toward structural: `core/mirror.py` MirrorView (I6), `ops/import_lint.py`+CI (I2 static), digest `Source` (I9), `derived_from`+acyclicity+`core/recursion.py` (I10); gaps CLOSED G1/G2/G3/G6, OPEN G4/G9/G10/G11. **Dream R&D detour — FEATURE-FLAG OFF (`[dream_rnd] enabled=false`, `core/dreaming/rnd.py` hard boundary), NOT wired into `scheduler/cron.py`** (live path still the Phase-7 `Dreamer`): R0 interpreter panel + R1 adjudicator built; R2/R3/R4 NOT. Do NOT flip the flag or wire R&D into cron without a deliberate R&D session. `core/stores/derived.py` `DerivedStore` (SQLite) writes `interpreted` provenance ONLY (no `provenance` param — §8 firewall, structural); idempotent content-ids + `reset()` = regenerable. `VectorStore.all_rows(provenances=…)` for clustering. `core/dreaming/` deterministic NumPy clustering (note centroids → cosine single-linkage) + `Dreamer` (clusters the AUTHORED mirror, **mirror-not-oracle**, injectable synthesizer, grounding self-check → INTERPRETED dreams). `core/curator/` `Curator` **flags, never rewrites authored** (§8): near-dup candidates + orphan-prune candidates (deterministic) + contradiction (injectable judge seam, **deferred** without a detector). `scheduler/cron.py` wires `dream`/`curate` → synthesis tier; the Phase-3 foreground gate (`HEAVY_TIERS`) makes trough-only structural. `[dreaming]` config + `numpy` dep. ⚠️ Live synthesis (`qwen3.6:27b`) NOT pulled → `test_dreaming_live` skips. ⚠️ Phase 4 empirical `-m podman` still pending — podman machine won't boot here; see `docs/runbook.md` → "Sandbox runtime". **Hardening pass (2026-06-26, NOT a phase — main build NOT advanced, dream R&D flag OFF):** worked the invariant catalog (`docs/WHITEPAPER-FORMAL-PROPERTIES.md`) — promoted I6 structural (`core/mirror.py` `MirrorView`), I2 static (`ops/import_lint.py` + CI), I9 decidable (digest `Source`), I10 acyclic (`derived_from` + cycle-refused insert, `core/recursion.py`); FSM-checked I8 (loader) + I12 (gate, honest G_now); G6 aging (`scheduler/queue.py`); G7 bounds declared; G8 preorder retired; Hypothesis property tests. **183 logic pass (+31)**; runtime behavior unchanged. Gaps: CLOSED G1/G2/G3/G6, OPEN G4 + new G9/G10/G11 — see `docs/PROGRESS.md`. **Dream R&D detour (2026-06-26, FEATURE-FLAG OFF — NOT a phase, NOT wired into cron):** built R0 interpreter panel (`core/dreaming/graph.py`+`interpreters.py`: registry of deterministic method-specialists over MirrorView) + R1 evidence-based adjudicator (`core/dreaming/adjudicator.py`: confidence-ordered dream log, c=γ^d·g·(1+λ(|Agr|−1)), agreement=multiplier-not-vote, INTERPRETED-only). Hard boundary `core/dreaming/rnd.py` (`[dream_rnd] enabled=false`). R2/R3/R4 NOT built. **194 logic pass (+11)**; live path still uses Phase-7 `Dreamer`. Next: Phase 8 (the airlock, AWS Zone C) — needs §20.7 (AWS acct/region/TF state) + §20.9 (Lambda vs Fargate) from owner. Phase 6b voice optional (§20.11). Update this line as phases complete.
```

