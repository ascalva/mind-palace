---
type: track
slug: agentic-loop
title: Agentic loop — AL-1 / AL-2 / AL-3
status: active
warrant: null
audit_refs: []
dod:
  - AL-1 the three actor profiles + the zone-boundary lattice test (bp-086)
  - AL-2 / AL-3 exhaust ⊂ dialogue + origin(e) delivered and wired (bp-087 / bp-088)
backlog_deskcheck: profile constructors + zone lattice test + exhaust⊂dialogue + origin(e); confirm delivered, not just sealed (bp-086/087/088)
links:
  - docs/design-notes/agentic-loop.md
---
# Track — Agentic loop (AL-1 / AL-2 / AL-3)

The identity card for the agentic-loop track. **Scope:** the three actor profiles,
the zone-boundary lattice, and the `exhaust ⊂ dialogue` / `origin(e)` relations.
Members are the artifacts declaring `track: agentic-loop` (the design note + plans
bp-086/087/088, all sealed).

**Definition of done:** the profile constructors, the zone lattice test, and the
`exhaust ⊂ dialogue` + `origin(e)` machinery are delivered AND wired — not merely
sealed (wiring was unverified at seal).

**Owed deskcheck (standing):** demo the profile constructors + zone lattice test +
`exhaust ⊂ dialogue` + `origin(e)`; confirm delivered, not just sealed (bp-086/087/088).
