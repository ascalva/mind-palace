<!-- GENERATED by scripts/handoff.py — do not hand-edit -->

# Handoff — role:orchestrator

> A **DERIVED** view of the artifact tree, regenerated by `scripts/handoff.py` and never hand-edited. It carries **no commit sha and no generation timestamp** by design (the §2.9 idempotence pin): two renders of an unchanged tree are byte-identical, so regenerating is a no-op and `git log` stays the one derived view of commits. Judgement lives in the seat journal; measurements live in the readings log; neither is restated here.

## The answer

- **Unit in flight:** bp-123 — the per-machine overlay becomes `config/ouroboros.toml`, and cannot be silently lost
- **Next action:** `/resume bp-123`
- **Blocking unknowns:** none

## Units of work

| plan | status | phase | title |
|---|---|---|---|
| bp-095 | ready | build | CI-4: the S↔F code↔design lens (M-C7, read-only survey) |
| bp-111 | ready | build | the dead-man inversion: health decays instead of being asserted |
| bp-112 | ready | build | the teeth: budgets that are enforced, and a kill that is bounded and loud |
| bp-113 | ready | build | the code lanes compute out-of-process and land in the supervisor |
| bp-114 | ready | build | the vault lane computes out-of-process; its five-store landing becomes one step |
| bp-116 | ready | build | P2: the process manager — residency becomes a kernel fact |
| bp-117 | ready | build | P3: the equivalence gate (the owner-ruled cutover precondition) |
| bp-118 | ready | build | P4: the embedder cuts over first (biggest win, smallest risk, provable) |
| bp-119 | ready | build | P5: the chat tiers, per-tier, each reversible (PARKED on V-B) |
| bp-123 | in-progress | build | the per-machine overlay becomes `config/ouroboros.toml`, and cannot be silently lost |
| bp-124 | ready | build | the orchestrator seat substrate and its handoff generator |
| bp-125 | ready | build | migrate the live brief into the seat, and re-home its rules |
| bp-126 | ready | build | the cutover: clause (e′), the re-point, and the brief's retirement |
| bp-127 | ready | build | the fresh-agent test made executable: F1b, F1c, and the F2 drill |

Status tally: complete 107 · in-progress 1 · ready 13 · superseded 6

## Awaiting the owner

- bless (proposed→ready): none
- ratify (draft notes): 41 — alignment-subsystem, attestation-layer, authorship-distance-axis, core-integrity, dialogue-ingest-and-recursion, dn-headless-daemon-secret-bootstrap …
- answer (open owner questions): 29 — oq-0003, oq-0004, oq-0005, oq-0006, oq-0007, oq-0008, oq-0009, oq-0010 …

## Deskchecks owed

| track | phase |
|---|---|
| Agentic loop | deskcheck-pending |
| Fiber geometry | deskcheck-pending |
| Inner/outer core | deskcheck-pending |
| Ops | build |
| Sync/diac dreamers | deskcheck-pending |

## Open findings

26 open — most recent: finding-0230, finding-0231, finding-0232, finding-0233, finding-0234, finding-0235

## Readings (MEASURED — latest per command)

| when | command | result |
|---|---|---|
| 2026-07-27T03:36Z | `uv sync --frozen --extra dev` | ok — dev extras resolved in a fresh worktree venv |

## Queue (an input, never the substrate)

- queue: a live probe (MEASURED), deliberately not carried in the committed rendering — run `uv run scripts/handoff.py --role <role>` for the pane

## The narrative

Judgement lives in `docs/roles/orchestrator/journal.md` — read its **authoritative segment**: the latest `## CAPSULE — <date>` heading plus every entry above it. This rendering deliberately restates none of it.

_Derived by `scripts/handoff.py` from artifact front matter + the seat's readings log. Never hand-edit; edit the sources and regenerate._
