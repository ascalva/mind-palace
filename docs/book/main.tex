% =============================================================================
% The Ouroboros Design Manual
% A derived projection of the ratified record and the codebase (dn-agent-workflow
% §3). It asserts nothing without a cited source: a design claim carries an
% artifact id via \artifact{}; an implementation claim carries code by path@ref via
% \coderef{}. Snippets are copies annotated `source: path@ref`. Notation is defined
% once in notation.tex. Draft notes are barred (dn-agent-workflow §3); draft theses
% are forward-referenced, never cited (see docs/findings/finding-0117.md).
%
% TOOLCHAIN: latexmk (pdflatex). tectonic — the plan's preferred default — is not
% installed on the build host; latexmk is the recorded fallback (bp-077 §3 Q1,
% SYNC.md). Build:  latexmk -pdf main.tex
% =============================================================================
\documentclass[11pt,oneside]{book}
\input{preamble}
\input{notation}

\title{The \Ouroboros{} Design Manual}
\author{A derived projection of the ratified record\\[2pt]
  \small compiled under the scribe contract (\texttt{contract: scribe})}
\date{Edition \gitref}

\begin{document}
\frontmatter
\maketitle
\tableofcontents

\mainmatter
\include{chapters/01-philosophy}
\include{chapters/02-architecture}
\include{chapters/03-mathematics}
\include{chapters/04-intuition}
\include{chapters/05-future-work}
\end{document}
