# Journal — bp-062 (the helix detector — GATED on uuid-identity)

## 2026-07-17 — graduated (proposed, GATED), not yet started
Minted by /graduate from RATIFIED `dn-connectivity-instruments` CN-6. Status `proposed`. **GATED on
uuid-identity landing** (owner-confirmed, D3) — awaits the owner's `proposed → ready` blessing.

**The gate is per-item:**
- **Item 10** (combinatorial detector + pitch functional on a **synthetic** gain graph) needs **no π** —
  buildable NOW. Proves the forced-helix theorem, spatial-only ⇒ flat, nonzero-pitch criterion.
- **Item 11** (real-corpus idea quotient via π) **requires uuid-identity** — the builder STOPS + parks it if
  π is absent (sanctioned partial completion; item 10 ships).

**Owner's stated default (honored):** the whole plan waits behind uuid-identity. Surfaced option: bless item 10
forward independently for early value (CN-6's "synthetic first, real once π lands"). The call is the owner's.

**Grounding carried in the plan:**
- ℤ-primary gain SIDESTEPS finding-0090 (proper-time erratum); the per-stratum refinement waits on it.
- The helix criterion is **nonzero pitch**, NOT snapshot-invisibility (the explicit CN-6 bug to avoid).
- π's integrity clause: **precision > recall** — a false merge manufactures a phantom helix.
- The forced-helix theorem's engine = the spine's constructed acyclicity (`SpineCycleError`).
- v1 combinatorial only; the U(1)/spectral route stays behind the magnetic lane's gate (Q6).

Estimate opus/180k.
